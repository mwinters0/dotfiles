-- vim: ts=2:sw=2:expandtab

-- Centered "zen mode"

return {
  {
    "https://github.com/shortcuts/no-neck-pain.nvim",
    version = "^2",
    opts = {
      -- width is screen width, so need to account for UI
      width = 115,
    },
  },
}
