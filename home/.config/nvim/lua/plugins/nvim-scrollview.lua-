-- vim: ts=2:sw=2:expandtab

-- https://github.com/dstein64/nvim-scrollview/blob/main/doc/scrollview.txt
-- :h scrollview-configuration

return {
  {
    'https://github.com/dstein64/nvim-scrollview',
    opts = {
      -- Interact with stuff "under" the scrollbar (e.g. signs)
      -- via Ctrl-left-click.  (Default: 'right')
      mouse_secondary = 'c-left',
      visibility = 'always', -- default 'overflow'
    },
  }
}
